org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.enableJetifier=true

# Set Gradle User Home to user-writable location (default)
# This prevents trying to write to Program Files
org.gradle.user.home=C\:\\Users\\dipmu\\.gradle

# Set JAVA_HOME for Gradle (if not set in environment)
# Update this path if your Android Studio JDK is in a different location
org.gradle.java.home=C\:\\Program Files\\Android\\Android Studio\\jbr

# Try to disable JDK image transformation
android.builder.sdkDownload=false

# Additional properties to help with JDK image transformation issues
org.gradle.daemon=true
org.gradle.parallel=true
# Temporarily disable caching to avoid corrupted cache issues
org.gradle.caching=false

# Disable JDK image transformation if causing issues
# Uncomment the line below if the error persists after cleaning cache
# android.disableAutomaticComponentCreation=true
