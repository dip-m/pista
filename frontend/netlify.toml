[build]
  publish = "build"
  command = "npm run build"
  # If deploying from root directory, set base to "frontend"
  # base = "frontend"

[build.environment]
  NODE_VERSION = "18"
  # Set this in Netlify dashboard → Site settings → Environment variables
  # REACT_APP_API_BASE_URL = "https://web-production-f74f5.up.railway.app"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
